apiVersion: v2
name: poddeck
description: PodDeck - Kubernetes Management Dashboard (Control Plane)
type: application
version: 0.1.0
appVersion: "1.0.0"

dependencies:
  - name: postgresql
    version: "~16.4"
    repository: https://charts.bitnami.com/bitnami
    condition: postgresql.enabled
